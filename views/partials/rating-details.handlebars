<section class="card">
  <a href="/song/{{id}}">
    {{#each RatingSong}}
    <header>{{song}}</header>
    {{/each}} 

    <div class="card-body" style="color: white;">
      <p style="font-size:100px">{{rating}}</p>
    </div>
  </a>
</section>