<section class="card">
  <a href="/song/{{id}}">
    {{#each RatingSong as |song|}}
    <header>{{song.song}}</header>
    {{/each}} 

    <div class="card-body top-songs" style="color: white;">
      <p style="font-size:100px">{{rating}}</p>
    </div>
  </a>
</section>