<section class="card">
  <a href="/song/{{id}}">
    {{#each RatingSong}}
    <header>{{song}}</header>
    {{/each}} 

    <div class="card-body col-lg-12">
      <p>{{rating}}</p>
    </div>
  </a>
</section>